
#include "include.h"
/*
int main() {
	// 对比返回0 1 -1 源大于对象则为1 
	string s1("aab");
	string s2("aac");
	int r1 = s1.compare(s2);
	cout << s1 << " " << s2 << " " << "->" << r1;
	cout << endl;

	// 插入(当前值的前面插入,插入几位,什么字符)
	string s3("heo");
	s3.insert(2, 2, 'l');
	cout << s3 << endl;

	//删除(从哪开始删，删几位) 
	string s4("hello woooorld");
	s4.erase(7, 3);
	cout << s4 << endl;
	//区间删除 
	string s5("hello woooorld");
	s5.erase(s5.begin() + 2, s5.begin() + 9);
	cout << s5 << endl;

	// 查找
	string s6("hello woooorld");
	cout << s6.find("oooo") << endl; // 返回第一次查找到的下标 
	cout << s6.find("oooo", 6) << endl;  // 从第n位开始查找
	cout << s6.rfind("o") << endl; // 从右边开始查找 返回第一次查找到的下标 
	
	// 获取字串
	string s7("hello woooorld");
	string Substr = s7.substr(0,5);
	cout << Substr << endl;
	return 0;
}
*/